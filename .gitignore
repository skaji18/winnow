node_modules/
web/dist/
web/dist-next/
*.log
.DS_Store

# Local runtime state (created at runtime in ~/.winnow by default)
.winnow/
winnow.db
winnow.db-*
scratch/
*.local

# Demo GIF recording intermediates (only docs/assets/*.webp is committed)
demo/.home/
demo/.rec/
